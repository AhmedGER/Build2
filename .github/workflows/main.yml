name: 🚀 Auto Build Minecraft Chat Hider Pack

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-everything:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📦 Checkout Repository
      uses: actions/checkout@v3
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 📝 Create build.py
      run: |
        cat > build.py << 'ENDPYTHON'
        #!/usr/bin/env python3
        import json
        import zipfile
        from pathlib import Path

        PACK_NAME = "Chat-Hider-Pack"
        PACK_DESCRIPTION = "يخفي رسائل الدردشة في ماين كرافت بيدروك"
        UUID_HEADER = "1642f509-51c7-4aec-93f3-fcd57acee38a"
        UUID_MODULE = "27d1ca2d-a89e-4be9-aeef-ff188efa5b04"

        def create_manifest():
            return {
                "format_version": 2,
                "header": {
                    "name": PACK_NAME,
                    "description": PACK_DESCRIPTION,
                    "uuid": UUID_HEADER,
                    "version": [1, 0, 0],
                    "min_engine_version": [1, 21, 0]
                },
                "modules": [
                    {
                        "type": "resources",
                        "uuid": UUID_MODULE,
                        "version": [1, 0, 0]
                    }
                ]
            }

        def create_hud_screen():
            return {
                "namespace": "hud",
                "root_panel": {
                    "modifications": [
                        {
                            "array_name": "controls",
                            "operation": "remove",
                            "where": { "name": "chat_panel" }
                        }
                    ]
                },
                "hud_content": {
                    "modifications": [
                        {
                            "array_name": "controls",
                            "operation": "remove",
                            "where": { "name": "chat_panel" }
                        }
                    ]
                }
            }

        def create_chat_screen():
            return {
                "namespace": "chat",
                "chat_screen_content": { "type": "panel", "visible": False },
                "chat_text_panel": { "type": "panel", "visible": False },
                "chat_background": { "type": "image", "visible": False },
                "send_button": { "type": "button", "visible": False }
            }

        def build_pack():
            print("Building resource pack...")
            temp_dir = Path("temp_pack")
            ui_dir = temp_dir / "ui"
            dist_dir = Path("dist")
            temp_dir.mkdir(exist_ok=True)
            ui_dir.mkdir(exist_ok=True)
            dist_dir.mkdir(exist_ok=True)

            with open(temp_dir / "manifest.json", "w", encoding="utf-8") as f:
                json.dump(create_manifest(), f, indent=2, ensure_ascii=False)
            with open(ui_dir / "hud_screen.json", "w", encoding="utf-8") as f:
                json.dump(create_hud_screen(), f, indent=2)
            with open(ui_dir / "chat_screen.json", "w", encoding="utf-8") as f:
                json.dump(create_chat_screen(), f, indent=2)

            pack_icon_data = bytes.fromhex(
                "89504e470d0a1a0a0000000d49484452000000010000000108060000001f15c4"
                "89000000a4944154789c01990066ff0003010100ff0003010100ff0003010100"
                "ff0003010100ff0003010100ff0003010100ff0003010100ff0003010100ff00"
                "03010100ff0003010100ff0003010100ff0003010100ff0003010100ff000301"
                "0100ff0003010100ff0003010100ff0003010100ff0003010100ff0003010100"
                "ff0003010100ff0003010100ff0003010100ff0003010100ff0003010100ff00"
                "03010100ff0003010100ff0003010100ff0003010100ff00fe0d9c24cf000000"
                "0049454e44ae426082"
            )
            with open(temp_dir / "pack_icon.png", "wb") as f:
                f.write(pack_icon_data)

            zip_path = dist_dir / f"{PACK_NAME}.mcpack"
            with zipfile.ZipFile(zip_path, "w", zipfile.ZIP_DEFLATED) as zipf:
                for file in temp_dir.rglob("*"):
                    if file.is_file():
                        arcname = file.relative_to(temp_dir)
                        zipf.write(file, arcname)

            import shutil
            shutil.rmtree(temp_dir)
            print(f"Build complete: {zip_path}")
            print(f"Size: {zip_path.stat().st_size / 1024:.2f} KB")

        if __name__ == "__main__":
            build_pack()
        ENDPYTHON
        chmod +x build.py
    
    - name: 📝 Create README.md
      run: |
        cat > README.md << 'EOF'
# Chat-Hider-Pack

![Minecraft](https://img.shields.io/badge/Minecraft-Bedrock-green?style=for-the-badge&logo=minecraft)
![Version](https://img.shields.io/badge/Version-1.21.114+-blue?style=for-the-badge)
![License](https://img.shields.io/badge/License-MIT-yellow?style=for-the-badge)

**حزمة موارد لإخفاء رسائل الدردشة في ماين كرافت بيدروك**

## ✨ المميزات
- 🚫 إخفاء كامل للدردشة من HUD
- 💬 يخفي جميع رسائل اللاعبين ورسائل النظام
- 🎮 متوافق مع Minecraft Bedrock 1.21.114+
- ⚡ بناء تلقائي عبر GitHub Actions
- 🔧 يستخدم Modifications API الرسمية من Mojang

## 📥 التحميل والتثبيت
... (باقي النص كما كتبته أنت)
EOF
    
    - name: 📝 Create .gitignore
      run: |
        cat > .gitignore << 'ENDGITIGNORE'
*.py[cod]
.Python
env/
venv/
dist/
temp_pack/
.DS_Store
Thumbs.db
.vscode/
.idea/
ENDGITIGNORE
    
    - name: 📝 Create LICENSE
      run: |
        cat > LICENSE << 'ENDLICENSE'
Copyright (c) 2024 SIGMAxox
Permission is hereby granted, free of charge, to any person obtaining a copy...
ENDLICENSE
        echo "✅ All project files created"
    
    - name: 🔨 Build Resource Pack
      run: python build.py
    
    - name: 📤 Upload Artifact
      uses: actions/upload-artifact@v3
      with:
        name: Chat-Hider-Pack
        path: dist/Chat-Hider-Pack.mcpack
        retention-days: 90
    
    - name: 🎉 Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: "🎮 Chat-Hider-Pack v${{ github.run_number }}"
        body: |
          ## 🚀 Release v${{ github.run_number }}
          ### ✨ ما الجديد
          - ✅ متوافق مع Minecraft Bedrock 1.21.114+
          - ✅ يخفي جميع رسائل الدردشة من HUD
          - ✅ يستخدم Modifications API الرسمية
          - ✅ سهل التثبيت والاستخدام
        files: dist/Chat-Hider-Pack.mcpack
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 🎊 Commit Generated Files
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add -A
        git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Auto-generated by SIGMAxox"
        git push || echo "Nothing to push"
    
    - name: ✅ Build Complete
      run: |
        echo "════════════════════════════════════════"
        echo "  ✨ Build Complete! ✨"
        echo "════════════════════════════════════════"
        echo "📦 Package: Chat-Hider-Pack"
        echo "👨‍💻 Developer: SIGMAxox"
        echo "🎮 Version: Minecraft Bedrock 1.21.114+"
        echo "════════════════════════════════════════"
